# Wely environment variables
# Copy to .env and adjust values. Variables prefixed with VITE_ are
# available at build time via import.meta.env.VITE_*

VITE_API_URL=http://localhost:3000
VITE_THEME=light
